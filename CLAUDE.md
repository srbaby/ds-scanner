# X-Plan 波段扫描系统 · AI 协作入口

> **这是索引，不是全文。** 详细规范在 `docs/`，本文件只保留：读什么、红线、以及"怎么不丢信息"。
> 收到任何需求：**先按下方顺序读文档 → 再读相关源文件 → 再动手**。
> **用户指令优先于本文件与 `docs/`。**
>
> **性质**：与主投资体系完全隔离的影子交易实验系统。
> 每日尾盘扫描 18 只 ETF，四维评分产出目标仓位清单，Bark 推送 + 网页看板，人工执行。

---

## 一、读这些（按需，不必全读）

| 文档 | 什么时候必须读 |
| --- | --- |
| [X-Plan.md](X-Plan.md) | **交易规则的唯一出处（canonical）**。开仓/止损止盈/仓位/评分/毕业熔断，一切规则只看它。其章节标题即索引，本文件不再维护第二份 |
| [docs/01-业务意图.md](docs/01-业务意图.md) | 判断"这段代码/这个数字是不是有意设计"的唯一依据。碰计算、字段语义、或觉得某处"像 bug"之前 |
| [docs/02-系统架构.md](docs/02-系统架构.md) | 碰链路、数据流、Gist 契约、触发排班、环境变量前 |
| [docs/03-实现约束.md](docs/03-实现约束.md) | **碰任何代码前** |
| [docs/04-开发纪律.md](docs/04-开发纪律.md) | 提交前对照检查清单 |
| [docs/05-未实现项.md](docs/05-未实现项.md) | 觉得"这里少了个功能"时先查，多半是刻意不做。**三条方法论待办 M-1/M-2/M-3 记在这篇第三节** |

**只有这五篇加一份方法论。** 没有决策日志、没有规则速查索引——理由见第三节。

---

## 二、红线（违反即打回，不必讨论）

1. **规则只在 `X-Plan.md`。** 任何文档、任何代码注释都不得复述交易规则。
   要改规则，**先改 `X-Plan.md` 再改代码**，不许代码先行、文档追认。
2. **改了 `X-Plan.md` 就要升方法论版本**，且 `VERSION.json` / `X-Plan.md` / `Prompt.md`
   三处一致（`validate_document_versions()` 会卡）。
3. **扫描器的 `decision` 是唯一权威。** AI 只做非权威审计且当前未启用，
   不得让 AI 修改评分、等级或操作。
4. **静默降级是本项目的头号事故源。** 数据缺失/异常时，宁可让它显眼地失败或显式标注，
   **绝不给一个"看起来合理"的默认值**。已经因此吃过三次亏（MA20 复权、政策衰减、政策接线）。
5. **政策事件只进四维评分的政策催化位**，不碰 `policy` 总分，因此不能触发也不能压制
   `RISK_STOP`，也不能绕开量比 / 现价>MA20 等硬条件。
6. **交易台账 append-only**，从不原地改历史。纠错追加 `CORRECT_REASON`，
   撤销追加 `REVERSE_EVENT`。
7. **前端脚本必须是普通 `<script>`**，不能用 `type="module"`（`file://` 要能直接打开）。
8. **改前端加载方式或接口形状，必须同步改 `js/test_*.js`**——它是 `scan.yml` 第一步，
   红了当天整个扫描不跑，且不会通知任何人。
9. **`scan.yml` 里政策步骤必须在 `ds_scanner.py` 之前**，否则政策影响不了当天建议。
10. **不擅自补功能、不擅自删代码或文件。** 觉得缺什么 → 说出来、给建议、等裁决。
    删除由大亨拍板。
11. **实时状态只在 Gist。** 本地 `data/*.json` 是历史快照，不可据此判断当前持仓或分数。
    可用 `gh api gists/<id>` 直接读真数据——**别拿快照做判断**。
    但**真实 Gist 只做只读核验**，未经用户明确授权不得写入。
12. **不得把 Token、Gist ID 或实时账户数据写入仓库、文档、日志或回复。**

---

## 三、防信息丢失机制（这套的存在理由）

**已经发生过的事故**：

- 2026-07-27 发现 CLAUDE.md 的「交易规则速查索引」是 v2.x 化石——v3.0 重写方法论时没同步，
  表里 10 个概念有 9 个在 `X-Plan.md` 里**零命中**，模块号几乎全错。
  它给规则造了第二个出处，而**烂索引比没索引更坏**：它把人导向错的地方，却看不出错。
- 同日发现那份 2026-07-10 的死代码候选清单在 **2.5 周内失效 83%**
  （6 条候选里 5 条早已被删），而作为一份静态文档，**没有任何机制会提醒读者这一点**。
  该文件已删除（内容进 `docs/05` 与 `docs/03` 1.5，原文在 git 里）。
- 同日发现毕业/熔断阈值——决定何时上真钱的规则——只以魔法数字存在于 `observe.py`，
  方法论文档零记载。**根因不是记性差，是判断依据没有被固化。**

因此立下三条：

### 3.1 每类信息只有一个归宿

| 是什么 | 写到哪 |
| --- | --- |
| **交易规则**——怎么开仓、怎么止损、评几分、什么时候毕业 | **`X-Plan.md`，唯一出处** |
| **业务逻辑**——为什么这么设计、这个数字回答什么问题 | **`docs/01`，唯一出处** |
| **当前实现长什么样**——链路、分层、数据契约 | `docs/02` |
| **反直觉的实现**——看着像 bug 其实是有意的 | 代码注释 **+** `docs/03`，两边都写 |
| **踩过什么坑、立了什么规矩** | `docs/03` / `docs/04` |
| **确实还没做的** | `docs/05` |
| **为什么改了这行代码** | **git，不用人维护** |

### 3.2 不留归档副本，历史交给 git

改文档就直接改，不留 `-日期` 副本、不建 archive 目录。
**归档层本身也需要维护，而它一旦烂掉是看不出来的。**

```bash
git log --oneline -- docs/01-业务意图.md     # 找到改之前那次提交
git show <hash>:docs/01-业务意图.md           # 看当时全文
git log --diff-filter=D -1 -- <被删的文件>    # 找删除点，再 git show <hash>^:<path>
```

**代价如实说**：翻 git 比读一份现成归档费劲。**所以更要把"为什么"写进正文本身**
——需要考古才能搞懂的规矩，就是正文没写到位。

### 3.3 给 AI 的话

> 你读到的文档可能不完整，代码里可能有文档没记的设计。
> **发现代码与文档冲突时，默认代码是对的、文档是漏的**——先问，不要按文档去"修正"代码。
> 2026-07-27 那次「AI 每日自动分析」的描述失实就是如此：`js/app.js` 早已正确处理
> `enabled:false`，烂的是文档。

---

## 四、项目速览

```
X-Plan/
├── CLAUDE.md              # 本文件（索引）
├── X-Plan.md              # 方法论正文，交易规则唯一出处
├── Prompt.md              # AI 审计提示词契约
├── VERSION.json           # 统一版本清单
├── docs/                  # 规范全文，只有 01-05 五篇
├── automation/
│   ├── ds_scanner.py          # 主扫描：评分 → 权威决策 → 报告
│   ├── policy_research/       # 政策事件流水线（collect→extract→score→compare）
│   │   ├── ai_classify.py         # 事件归类判 ±（两趟调 DeepSeek），失败退回关键词
│   │   └── check_etf_coverage.py  # 核对每个 ETF 板块 ≥2 个供数的源
│   ├── generate_dashboard.py  # 写 Gist dashboard.json
│   ├── send_report.py         # Bark 推送
│   ├── observe.py             # 夜间观察器：台账/快照/统计
│   └── test_*.py              # 回归测试（scan.yml 门禁）
├── data/etf_base_config.json  # 板块政策基础分，唯一需手工维护的数据
├── data/policy_research/sources.json  # 31 个政务/行业源，按 ETF 板块配齐
├── index.html · js/ · css/    # 前端（GH Pages，stock.bailuzun.com）
├── workers/ds-scan-trigger/   # Cloudflare Worker 触发器源码
└── .github/workflows/scan.yml
```

**两个子系统，别混谈**（详见 `docs/01` 第二节）：

- **扫描研判**（2.x 主干）：回答"今天该怎么操作"。挂了 = 当天没建议，属"少赚"。
- **验证评估**（3.x 新增，复杂度主要来源）：回答"这套方法论到底有没有用"。
  挂了 = 统计失真，可能导致错误的"能不能上真钱"判断。

**排班不在本仓库**：几点唤醒、唤醒哪个 action，全在 `srbaby/Master-Scheduler`
（独立私有仓库）。本仓库只管收到唤醒之后做什么。

---

## 五、版本记录（只留最近两条，更早的在 `plans/version-history-archive.md`）

| 版本 | 日期 | 核心变更 |
| --- | --- | --- |
| **v3.3 政策归类交给 AI + 源按 ETF 重构** | 2026-07-29 | 事件归到哪个板块、判 ± 改由 AI 判定（`X-Plan.md` 模块11），关键词表退为兜底；因 DeepSeek 无联网能力拆成两趟（选读 → 抓正文 → 判方向），失败整体退回关键词并在 report/Bark 显式标注。源从 15 个扩到 31 个、按 ETF 板块配齐（17 个板块全部 ≥2 个供数的源，采集 231→677 条），新增 `check_etf_coverage.py` 把口径变成可核对的数字。看板政策加减分可逐条穿透（标题+原文链接+日期+贡献分值） |
| **政策链路四连修** | 2026-07-29 | ① 一对半角引号造成 `compare_policy_decision.py` 语法错误，政策停摆 2 天 5 次扫描无人察觉——门禁加 `compileall`，政策状态进 Bark。② 年龄护栏把解析对的 2021 旧日期判成"没日期"、下游再兜底成今天，造出不死僵尸，3 个生效主题全由僵尸驱动。③ 4 个 rank-S 源哑掉（JS 跳转壳页 / 子域名没进白名单），HTTP 200 且 `error_count` 为 0 完全看不出来。④ 列表页 `MM-DD` 无年份日期解析不了，且爬父容器会抓到邻居的日期 |
