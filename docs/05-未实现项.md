# 第五篇 · 未实现项

> 本篇只记录**当前确实没有实现、刻意不实现，或仍等待用户裁决**的事项。
> 它不是实时状态页，也不保存完成记录；已完成或放弃的历史交给 Git。
>
> 涉及交易规则时只引用 `X-Plan.md`，不在本篇建立第二份规则。

---

## 一、当前未实现

| 编号 | 事项 | 当前行为与影响 | 决策状态 |
| --- | --- | --- | --- |
| F-1 | 熔断触发逻辑 | `automation/observe.py` 只计算部分进度，状态仍固定为 `normal`；即使数据达到方法论阈值，面板也不会自动切换为熔断 | 是否实现待用户裁决；阈值只看 `X-Plan.md` 模块 10 |
| F-2 | 政策事件台账持久化 | Actions 中的 `data/policy_research/events/*.jsonl` 是一次性工作区，跨次运行的事件去重与长期衰减缺少持久台账 | 建议迁入 Gist，但写入范围、冲突处理和迁移方式待裁决 |
| F-3 | 池外持仓风控 | 观察池外持仓不参与扫描评分，没有持仓逻辑分，也不会产生扫描器风控动作 | 当前依靠人工纪律；是否扩展扫描范围待裁决 |
| F-4 | 基础分配置损坏时强提醒 | `etf_base_config.json` 读取失败会打印警告并退回硬编码兜底值，Actions 仍继续运行 | 是否改为阻断或 Bark 强提醒待裁决 |
| F-5 | 规则外虚拟信号记账 | 系统只记录用户实际执行的影子盘交易，没有为所有合格信号另建纯虚拟样本 | 是否用于加速样本积累待裁决；不得通过放松正式门槛替代 |

---

## 二、刻意不实现

以下不是缺陷，除非用户先改变业务意图或方法论：

| 事项 | 当前决定 | 依据 |
| --- | --- | --- |
| 为增加样本放松硬条件 | 不做，会同时改变风控与验证口径 | `docs/01-业务意图.md`、`X-Plan.md` |
| 盘中多次调仓 | 不做，每日一次决策窗口 | `docs/01-业务意图.md` |
| 自动执行交易 | 不做，系统只生成清单 | `docs/01-业务意图.md` |
| 自动回写人工政策基础分 | 不做，事件偏移只在运行时叠加 | `X-Plan.md` 模块 11 |
| 让 AI 直接产出评分或操作 | 不做；AI 只参与政策事件归类，扫描器仍是权威决策源 | `docs/01-业务意图.md` 第四节 |
| 用关键词补回 AI 第一趟未选中的条目 | 不做，避免把 AI 已判为低价值的噪音重新放入事件集 | `docs/03-实现约束.md` |
| 把 Bark 全文改成摘要 | 暂不做，保留复制全文进行人工兜底的使用方式 | `docs/01-业务意图.md` |

---

## 三、方法论待决事项

下面是研究问题，不是已确认的工程缺陷。每次复议必须先从 Gist 重跑数据；表中的数字
只是 **2026-07-27 的冻结研究快照**，不得当作当前状态。

| 编号 | 问题 | 2026-07-27 研究快照 | 下一步与复议条件 |
| --- | --- | --- | --- |
| M-1 | 入场位置与趋势退出线较近，而另一侧风险退出空间更大，可能形成出场几何不对称 | `TREND_BREAK` 2 笔平均 `+2.07%`；`RISK_STOP` 1 笔 `-7.05%`。样本很小，但几何关系来自规则本身 | 用户需在“延迟武装趋势退出 / 改用波动容忍带 / 保持现状”之间裁决；任何调整都先改 `X-Plan.md` 并升版本 |
| M-2 | 高胜率、低赔率体系的统计边际尚未确立 | 19 笔平仓，胜率 `78.95%`，盈亏比 `0.6467`，Wilson 95% 区间下沿低于当时估算的盈亏平衡胜率 | 平仓笔数达到 30 时重算胜率区间与盈亏平衡点；此前不据此改规则 |
| M-3 | 资金利用反馈是否说明规则过严 | 当时快照显示胜率与平均仓位达到方法论规定的复查条件；低仓位同时可能是控制回撤的来源 | 用最新 Gist 数据逐项检查开仓、加仓、盈利钝化和换仓限制；复查不等于预设“应该多买” |

`M-1` 到 `M-3` 的状态仍是：**已记录，暂不修改方法论**。

---

## 四、工程清理候选

| 对象 | 当前证据 | 处理边界 |
| --- | --- | --- |
| `data/etf_base_config/PROMPT_FOR_GEMINI.md` | 仓库内未发现引用；同目录 `GEMINI_UPDATE_GUIDE.md` 承担现行说明 | 是否已被完全取代需用户确认，未授权前不删除 |

发现新的候选时，必须先证明其当前无调用、无文档责任、无外部工作流依赖，再提交给用户裁决。
