# X-Plan 波段扫描系统 · AI 协作入口

> **这是索引，不是全文。** 收到需求后先按本页定位正式文档，再读相关源文件。
> **用户指令优先于本文件与 `docs/`。**
>
> 项目性质：与主投资体系完全隔离的影子交易实验系统。每日尾盘扫描 ETF
> 精选池，四维评分产出目标仓位清单，经 Bark 与网页看板送达，由用户人工执行。

---

## 一、阅读路由

| 文档 | 什么时候必须读 |
| --- | --- |
| [X-Plan.md](X-Plan.md) | **交易规则的唯一出处（canonical）**。涉及开仓、止损止盈、仓位、评分、毕业或熔断时 |
| [docs/01-业务意图.md](docs/01-业务意图.md) | 判断某段代码、数字或反直觉行为是不是有意设计时 |
| [docs/02-系统架构.md](docs/02-系统架构.md) | 涉及链路、数据流、Gist 契约、触发排班或环境变量时 |
| [docs/03-实现约束.md](docs/03-实现约束.md) | **修改任何代码前** |
| [docs/04-开发纪律.md](docs/04-开发纪律.md) | 审查、验证和提交前 |
| [docs/05-未实现项.md](docs/05-未实现项.md) | 发现缺失能力或准备补功能时 |
| [docs/06-系统审查Prompt.md](docs/06-系统审查Prompt.md) | 触发全系统审查时（触发条件见下），不是日常必读 |

正式项目文档只有 01-05 五篇加一份方法论。`06` 是工具不是出处——它只提问题、
不定义规则，所有规则数值一律要求执行者现场从 `X-Plan.md` 读取。组件 README
只承担运行、接口和运维说明，不得成为业务规则或系统架构的第二出处。

**什么时候跑 `06`：** 整段喂给一个没有本项目上下文的高阶 AI，让它按三层框架体检。
满足以下任一条即应执行一次，不必等谁提醒：

1. `VERSION.json` 的 `methodology_version` 升位后——规则体系变了，`06` 第二层直接受影响。
2. 跨组件的系统性改造合并后（同一批动了扫描器/政策/前端/工作流中的两条以上链路）。
3. **准备从影子模式转实盘前必须跑一次**，这是不可跳过的一次。毕业观察线只回答
   "统计上够不够格"，`06` 回答"工程上敢不敢真金白银跑"，两者缺一不可。

审查结论按 `docs/` 的既有分工归位：业务意图进 `01`、架构进 `02`、实现约束进 `03`、
待决事项进 `05`。审查报告本身不入库，需要回溯时用 Git。

---

## 二、红线

1. **规则只在 `X-Plan.md`。** 要改规则，先改方法论再改代码，不许代码先行、文档追认。
2. **改了 `X-Plan.md` 就要升方法论版本。** `VERSION.json`、`X-Plan.md`、
   `Prompt.md` 三处必须一致，`validate_document_versions()` 会校验。
3. **扫描器的 `decision` 是唯一权威。** AI 每日审计当前未启用；AI 不得修改评分、
   等级或操作。
4. **静默降级是头号事故源。** 数据缺失或异常时必须显式失败或标注，不得返回一个
   看似合理的默认值。
5. **政策事件只进入四维评分的政策催化位。** 不碰持仓逻辑 `policy` 总分，
   不能触发或压制 `RISK_STOP`，不能绕开量比、现价高于 MA20 等硬条件。
6. **交易台账 append-only。** 纠错追加 `CORRECT_REASON`，撤销追加
   `REVERSE_EVENT`，不得原地改写历史。
7. **前端脚本必须是普通 `<script>`。** 不能改成 `type="module"`，要保证
   `file://` 直接打开可用。
8. **改前端加载方式或接口形状，必须同步修改前端测试。** 前端测试是
   `scan.yml` 第一道门禁。
9. **`scan.yml` 中政策步骤必须位于 `ds_scanner.py` 之前。**
10. **不擅自补功能，不擅自删除代码或文件。** 先说明影响和方案，等用户裁决。
11. **实时状态只在 Gist。** 本地 `data/*.json` 是历史快照；真实 Gist 只读核验，
    未经明确授权不得写入。
12. **不得把 Token、Gist ID 或实时账户数据写入仓库、文档、日志或回复。**

---

## 三、文档维护路由

| 信息类型 | 唯一维护位置 |
| --- | --- |
| 交易规则：怎么开仓、止损、评分、毕业 | `X-Plan.md` |
| 业务意图：为什么这样设计、字段回答什么问题 | `docs/01-业务意图.md` |
| 当前实现：链路、分层、数据契约、环境变量 | `docs/02-系统架构.md` |
| 反直觉实现、仍有效的事故约束 | 代码注释 + `docs/03-实现约束.md` |
| 审查、测试和提交纪律 | `docs/04-开发纪律.md` |
| 当前未实现且等待决策的事项 | `docs/05-未实现项.md` |
| 全系统审查的提问框架与触发条件 | `docs/06-系统审查Prompt.md` |
| 某次改动的过程和历史版本 | Git |
| 组件运行、接口和运维方法 | 对应目录 README，引用上面的正式出处 |

不留日期副本，不维护 changelog 文档，不建 archive 目录。需要回溯时使用 Git：

```bash
git log --oneline -- docs/01-业务意图.md
git show <hash>:docs/01-业务意图.md
git log --diff-filter=D -1 -- <被删除的文件>
```

发现文档与代码冲突时，默认代码反映当前实现、文档可能漏改；先核对意图和影响，
不要直接按旧文档改代码。

---

## 四、项目速览

```text
X-Plan/
├── CLAUDE.md                  AI 协作入口
├── X-Plan.md                  方法论，交易规则唯一出处
├── Prompt.md                  AI 审计提示词契约
├── VERSION.json               统一版本清单
├── docs/                      01-05 五篇正式说明，06 全系统审查 Prompt（工具）
├── automation/
│   ├── policy_research/       政策采集、归类、评分与影响对比
│   ├── ds_scanner.py          四维评分、权威决策与报告
│   ├── generate_dashboard.py  写入 Gist 看板数据
│   ├── send_report.py         Bark 推送
│   ├── observe.py             夜间观察器
│   └── test_*.py              Python 回归测试
├── data/
│   ├── etf_base_config.json   唯一需手工维护的政策基础分
│   └── policy_research/       政策来源、映射与运行产物
├── index.html · js/ · css/    前端
├── workers/                   Cloudflare Worker 触发器源码
└── .github/workflows/         扫描与观察工作流
```

系统分成两条职责不同的链路：

- **扫描研判**回答“今天如何操作”；失败意味着当天没有建议。
- **验证评估**回答“方法论是否有效”；失败会污染长期统计与转实盘判断。

触发排班由独立私有仓库 `Master-Scheduler` 维护，本仓库只定义被唤醒后的行为。

---

## 五、提交前

按 [docs/04-开发纪律.md](docs/04-开发纪律.md) 执行完整检查。至少包括：

```bash
npm run test:frontend
PYTHONPATH=automation python3 -m unittest discover -s automation -p 'test_*.py'
python3 -m compileall -q automation
git diff --check
```
